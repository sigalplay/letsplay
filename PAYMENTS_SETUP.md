# חיבור סליקה (Cardcom)

הממשק מוכן לקבל שלוש כתובות Checkout דרך משתני הסביבה שב-`.env.example`.

לפרסום אמיתי יש להוסיף שרת מאובטח שמבצע את הפעולות הבאות:

1. יוצר עסקה ב-Cardcom עבור מסלול חודשי, שנתי או מחיר השקה.
2. מקבל webhook מ-Cardcom ומוודא את חתימתו בצד השרת.
3. שומר במסד הנתונים את סטטוס המנוי ותאריך התפוגה של המשתמש.
4. מחזיר לאתר סטטוס מנוי מתוך החשבון המחובר.

אסור לשמור מספר מסוף, סיסמה או מפתח API במשתנה שמתחיל ב-`VITE_`, בקוד הדפדפן או ב-Git. משתני `VITE_CHECKOUT_*` מיועדים רק לכתובות ציבוריות. עד לחיבור השרת, כפתורי התשלום מציגים שהסליקה תיפתח בקרוב.
